steps:
- name: gcr.io/cloud-builders/docker
  args:
    - build
    - .
    - --file
    - ./Dockerfile
    - --build-arg
    - VITE_API_BASE=${_VITE_API_BASE}
    - --tag
    - ${_IMAGE}
images:
- ${_IMAGE}
